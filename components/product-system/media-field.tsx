"use client";

/**
 * Media controls for the Product Master. Identical to the Purchase module's
 * uploader/gallery (shared IndexedDB media store), so we re-export.
 */

export { MediaField, MediaGallery, mediaCount, useObjectUrl } from "@/components/purchase-system/media-field";
